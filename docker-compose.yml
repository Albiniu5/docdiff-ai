version: '3.8'

services:
  web:
    build: .
    container_name: docdiff-ai
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: always
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=production
